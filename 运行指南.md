# 狮山花园 — 运行指南

H5 + 微信小程序双端应用。后端 Flask，前端 uni-app（`Frontend-uni/`）。

## 一、启动后端

后端监听 `http://127.0.0.1:5000`，提供 `/v1/...` 接口（与 `Frontend-uni` 对齐）。

```bash
cd backend
source venv/bin/activate
python app.py
```
下面需要你进行前端的风格和布局，请你参考materia design
  的设计风格 
> **注意**：worktree 内的 `backend/venv` 可能损坏（python 缺执行权限）。如启动报
> `python: command not found` 或 `Permission denied`，重建虚拟环境：
> ```bash
> cd backend
> python3 -m venv venv
> source venv/bin/activate
> pip install -r requirements.txt
> python app.py
> ```

验证：`curl http://127.0.0.1:5000/` 返回 `Backend is running` 即正常。

## 二、编译小程序

```bash
cd Frontend-uni
npm install                 # 首次需要
npm run dev:mp-weixin       # 开发模式（watch），产物 → dist/dev/mp-weixin
npm run build:mp-weixin     # 生产构建，产物 → dist/build/mp-weixin
```

`dev:mp-weixin` 会持续监听源码变化并自动重新编译，微信开发者工具会自动刷新。

| 命令 | 用途 |
|------|------|
| `npm run dev:h5` / `build:h5` | H5 端调试 / 构建 |
| `npm run dev:mp-weixin` / `build:mp-weixin` | 小程序端调试 / 构建 |
| `npm run type-check` | TypeScript 类型检查 |

## 三、在微信开发者工具中打开

1. 打开微信开发者工具 → **导入项目**
2. 目录选择编译产物（WSL 路径在 Windows 下通过 `\\wsl.localhost` 访问）：
   ```
   \\wsl.localhost\Ubuntu\home\yaoyao\vibe\se\26-SE-Project\.worktrees\feat-miniprogram-migration\Frontend-uni\dist\dev\mp-weixin
   ```
3. **AppID**：填 `wx6f29122b119c9c6f`，或选"测试号"
4. **联调本地后端**：详情 → 本地设置 → 勾选 **"不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书"**，否则无法访问 `http://127.0.0.1:5000`

## 四、常见错误

| 报错 | 原因 / 处理 |
|------|------------|
| `INVALID_LOGIN, access_token expired` | 开发者工具自身登录态过期。点头像 → 退出登录 → 重新扫码登录 |
| `app.json ... iconPath ... 未找到` | 静态资源必须放在 `Frontend-uni/src/static/` 下（uni-app 输入目录为 `src/`）。已修复 |
| 所有接口 404 / 回退 Mock | 后端未启动，或 `src/services/api.ts` 的 `API_BASE_URL` 与实际地址不符 |
| `python: command not found` | worktree venv 损坏，按第一节重建虚拟环境 |

## 五、关键配置

- **API 地址**：`Frontend-uni/src/services/api.ts` → `API_BASE_URL`，当前为 `http://127.0.0.1:5000`
- **小程序页面 / tabBar**：`Frontend-uni/src/pages.json`
- **静态资源**：`Frontend-uni/src/static/`（图标、轮播图）
